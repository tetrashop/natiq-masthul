#!/bin/bash
echo "🧹 مدیریت منابع نطق مصطلح"

# پاکسازی کش npm
npm cache clean --force

# پاکسازی لاگ‌های قدیمی
find . -name "*.log" -type f -mtime +7 -delete

# پاکسازی فایل‌های موقت
find . -name "*.tmp" -type f -delete

# بررسی فضای دیسک
echo "📊 وضعیت فضای دیسک:"
df -h | grep -v tmpfs

echo "✅ پاکسازی کامل شد"
