#!/bin/bash

echo "🚀 شروع استقرار روی گیت‌هاب..."
echo "📁 مسیر جاری: $(pwd)"

# بررسی وجود گیت
if ! command -v git &> /dev/null; then
    echo "❌ Git یافت نشد. لطفا Git را نصب کنید."
    exit 1
fi

echo "✅ Git پیدا شد: $(git --version)"

# مقداردهی اولیه گیت
if [ ! -d ".git" ]; then
    echo "🔧 مقداردهی اولیه مخزن گیت..."
    git init
fi

# افزودن فایل‌ها
echo "📦 افزودن فایل‌ها به گیت..."
git add .

# کامیت تغییرات
echo "💾 ثبت تغییرات..."
git commit -m "🚀 نسخه ۳.۰.۰ نطق مصطلح - سیستم کامل هوش مصنوعی فارسی

• پردازش زبان فارسی پیشرفته
• سیستم کش هوشمند
• تحلیل احساسات و موجودیت‌ها
• رابط REST API کامل
• داشبورد مدیریت یکپارچه
• پشتیبانی از ۱۷۳ ویژگی NLP"

# نمایش وضعیت
echo "📊 وضعیت فعلی:"
git status

echo ""
echo "✅ آماده برای ارسال به گیت‌هاب!"
echo ""
echo "📝 دستورات بعدی:"
echo "   git remote add origin https://github.com/YOUR_USERNAME/natiq-masthul.git"
echo "   git branch -M main"
echo "   git push -u origin main"
echo ""
echo "🔗 آدرس مخزن را با آدرس واقعی خود جایگزین کنید."
